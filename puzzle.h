#ifndef PUZZLE_H
#define PUZZLE_H

#include <vector>
#include <string>

using namespace std;

class Puzzle
{
private:
    vector<int> board;

public:
    Puzzle(string &file_name);

    getline(inputFile, line);
    stringstream ss(line);
};

#endif // PUZZLE_H
